{
  "name": "getDandelionInfo",
  "description": "Returns current information about the Dandelion system intelligence, available agents, and services.",
  "examples": {
    "input": {
      "question": "What agents are available?",
      "questionCategory": "agents"
    }
  },
  "input": {
    "type": "object",
    "properties": {
      "question": {
        "description": "A question to ask the Dandelion system intelligence, if applicable.",
        "type": "string"
      },
      "questionCategory": {
        "default": "company",
        "enum": ["agents", "board", "company", "executive", "human remembrance project", "intelligences", "interface", "membership", "platform", "q", "registration", "roadmap", "services", "technology"],
        "type": "string"
      }
    },
    "required": ["question"]
  },
  "metadata": {
    "aliases": {
      "getDandelionAgents": {
        "proxyFor": "getDandelionInfo",
        "defaultInput": { "questionCategory": "agents" }
      },
      "getMembershipInfo": {
        "proxyFor": "getDandelionInfo",
        "defaultInput": { "questionCategory": "membership" }
      },
      "getRegistrationInfo": {
        "proxyFor": "getDandelionInfo",
        "defaultInput": { "questionCategory": "registration" }
      },
      "getHelpInfo": {
        "proxyFor": "getDandelionInfo",
        "defaultInput": { "questionCategory": "interface" }
      }
    }
  },
  "preferredProviders": ["internal"],
  "protocol": "http/1.1",
  "version": "1.0.0"
}
